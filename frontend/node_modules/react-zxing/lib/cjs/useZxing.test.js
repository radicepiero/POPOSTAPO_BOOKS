"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = require("@testing-library/react");
const react_2 = require("react");
const vitest_1 = require("vitest");
const useZxing_1 = require("./useZxing");
const { prepareWasm, detect, detectSkewedVideo } = vitest_1.vi.hoisted(() => ({
    prepareWasm: vitest_1.vi.fn().mockResolvedValue(undefined),
    detect: vitest_1.vi.fn().mockResolvedValue([]),
    detectSkewedVideo: vitest_1.vi.fn().mockResolvedValue([]),
}));
vitest_1.vi.mock("./prepareWasm", () => ({
    prepareWasm,
}));
vitest_1.vi.mock("./useBarcodeDetector", () => ({
    useBarcodeDetector: () => ({ detect }),
}));
vitest_1.vi.mock("./detectSkewedVideo", () => ({
    detectSkewedVideo,
}));
const createStream = () => {
    const track = {
        stop: vitest_1.vi.fn(),
        getCapabilities: vitest_1.vi.fn(() => ({})),
    };
    return {
        getTracks: () => [track],
        getVideoTracks: () => [track],
    };
};
const prepareVideoForScanning = () => {
    const video = document.querySelector("video");
    if (!video)
        throw new Error("video element not found");
    Object.defineProperties(video, {
        readyState: {
            value: HTMLMediaElement.HAVE_ENOUGH_DATA,
            configurable: true,
        },
        videoWidth: { value: 640, configurable: true },
        videoHeight: { value: 480, configurable: true },
    });
    return video;
};
const Scanner = ({ options }) => {
    const { ref } = (0, useZxing_1.useZxing)(options);
    return (0, react_2.createElement)("video", { ref });
};
(0, vitest_1.describe)("useZxing", () => {
    (0, vitest_1.beforeEach)(() => {
        vitest_1.vi.useFakeTimers({ shouldAdvanceTime: true });
        vitest_1.vi.stubGlobal("isSecureContext", true);
        vitest_1.vi.stubGlobal("navigator", {
            mediaDevices: {
                getUserMedia: vitest_1.vi.fn().mockResolvedValue(createStream()),
            },
        });
        HTMLVideoElement.prototype.play = vitest_1.vi
            .fn()
            .mockResolvedValue(undefined);
        prepareWasm.mockClear();
        detect.mockReset();
        detect.mockResolvedValue([]);
        detectSkewedVideo.mockReset();
        detectSkewedVideo.mockResolvedValue([]);
    });
    (0, vitest_1.afterEach)(() => {
        (0, react_1.cleanup)();
        vitest_1.vi.useRealTimers();
        vitest_1.vi.unstubAllGlobals();
    });
    (0, vitest_1.it)("starts the camera and reports decoded barcodes", async () => {
        const barcode = {
            rawValue: "978020137962",
            format: "ean_13",
            boundingBox: new DOMRectReadOnly(0, 0, 1, 1),
            cornerPoints: [
                { x: 0, y: 0 },
                { x: 1, y: 0 },
                { x: 1, y: 1 },
                { x: 0, y: 1 },
            ],
        };
        const onDecodeResult = vitest_1.vi.fn();
        detect.mockResolvedValue([barcode]);
        (0, react_1.render)((0, react_2.createElement)(Scanner, {
            options: {
                trySkew: false,
                timeBetweenDecodingAttempts: 50,
                onDecodeResult,
            },
        }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(prepareWasm).toHaveBeenCalledOnce();
            (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).toHaveBeenCalledOnce();
        });
        prepareVideoForScanning();
        await vitest_1.vi.advanceTimersByTimeAsync(100);
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(onDecodeResult).toHaveBeenCalledWith(barcode);
        });
    });
    (0, vitest_1.it)("does not start scanning while paused", async () => {
        (0, react_1.render)((0, react_2.createElement)(Scanner, { options: { paused: true } }));
        await vitest_1.vi.advanceTimersByTimeAsync(500);
        (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).not.toHaveBeenCalled();
        (0, vitest_1.expect)(prepareWasm).not.toHaveBeenCalled();
    });
    (0, vitest_1.it)("forwards camera setup errors", async () => {
        const onError = vitest_1.vi.fn();
        const cameraError = new Error("permission denied");
        vitest_1.vi.mocked(navigator.mediaDevices.getUserMedia).mockRejectedValue(cameraError);
        (0, react_1.render)((0, react_2.createElement)(Scanner, { options: { onError } }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(onError).toHaveBeenCalledWith(cameraError);
        });
    });
    (0, vitest_1.it)("uses an explicit device id when provided", async () => {
        (0, react_1.render)((0, react_2.createElement)(Scanner, { options: { deviceId: "back-camera" } }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).toHaveBeenCalledWith({
                audio: false,
                video: { deviceId: { exact: "back-camera" } },
            });
        });
    });
    (0, vitest_1.it)("forwards decode errors from the detector", async () => {
        const onDecodeError = vitest_1.vi.fn();
        const decodeError = new Error("decode failed");
        detect.mockRejectedValue(decodeError);
        (0, react_1.render)((0, react_2.createElement)(Scanner, {
            options: {
                trySkew: false,
                timeBetweenDecodingAttempts: 50,
                onDecodeError,
            },
        }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).toHaveBeenCalledOnce();
        });
        prepareVideoForScanning();
        await vitest_1.vi.advanceTimersByTimeAsync(100);
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(onDecodeError).toHaveBeenCalledWith(decodeError);
        });
    });
    (0, vitest_1.it)("does not retry skewed frames by default", async () => {
        (0, react_1.render)((0, react_2.createElement)(Scanner, {
            options: { timeBetweenDecodingAttempts: 50 },
        }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).toHaveBeenCalledOnce();
        });
        prepareVideoForScanning();
        await vitest_1.vi.advanceTimersByTimeAsync(100);
        (0, vitest_1.expect)(detect).toHaveBeenCalled();
        (0, vitest_1.expect)(detectSkewedVideo).not.toHaveBeenCalled();
    });
    (0, vitest_1.it)("retries skewed frames when enabled", async () => {
        (0, react_1.render)((0, react_2.createElement)(Scanner, {
            options: { trySkew: true, timeBetweenDecodingAttempts: 50 },
        }));
        await (0, react_1.waitFor)(() => {
            (0, vitest_1.expect)(navigator.mediaDevices.getUserMedia).toHaveBeenCalledOnce();
        });
        const video = prepareVideoForScanning();
        await vitest_1.vi.advanceTimersByTimeAsync(100);
        (0, vitest_1.expect)(detectSkewedVideo).toHaveBeenCalledWith(video, { detect }, vitest_1.expect.any(Number));
    });
});
