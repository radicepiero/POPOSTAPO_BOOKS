"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const { prepareZXingModule } = vitest_1.vi.hoisted(() => ({
    prepareZXingModule: vitest_1.vi.fn(() => undefined),
}));
vitest_1.vi.mock("barcode-detector/ponyfill", () => ({
    prepareZXingModule,
}));
const loadPrepareWasm = async () => {
    const { prepareWasm } = await Promise.resolve().then(() => __importStar(require("./prepareWasm")));
    return prepareWasm;
};
(0, vitest_1.describe)("prepareWasm", () => {
    (0, vitest_1.beforeEach)(() => {
        vitest_1.vi.resetModules();
        prepareZXingModule.mockReset();
        prepareZXingModule.mockReturnValue(undefined);
    });
    (0, vitest_1.it)("initializes the ZXing module once", async () => {
        const prepareWasm = await loadPrepareWasm();
        await Promise.all([prepareWasm(), prepareWasm()]);
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledTimes(1);
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledWith({
            fireImmediately: true,
        });
    });
    (0, vitest_1.it)("awaits async module initialization", async () => {
        prepareZXingModule.mockReturnValue(Promise.resolve());
        const prepareWasm = await loadPrepareWasm();
        await prepareWasm();
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledOnce();
    });
    (0, vitest_1.it)("routes wasm requests through a custom url", async () => {
        const prepareWasm = await loadPrepareWasm();
        await prepareWasm({ wasmUrl: "/custom/zxing_reader.wasm" });
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledOnce();
        const firstCall = prepareZXingModule.mock.calls[0]?.[0];
        (0, vitest_1.expect)(firstCall).toBeDefined();
        const { overrides } = firstCall;
        (0, vitest_1.expect)(overrides.locateFile("zxing_reader.wasm", "/prefix/")).toBe("/custom/zxing_reader.wasm");
        (0, vitest_1.expect)(overrides.locateFile("other.js", "/prefix/")).toBe("/prefix/other.js");
    });
    (0, vitest_1.it)("reuses the prepared module for the same custom URL", async () => {
        const prepareWasm = await loadPrepareWasm();
        await prepareWasm({ wasmUrl: "/zxing_reader.wasm" });
        await prepareWasm({ wasmUrl: "/zxing_reader.wasm" });
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledOnce();
    });
    (0, vitest_1.it)("reuses a custom URL when useZxing calls prepareWasm without wasmUrl", async () => {
        const prepareWasm = await loadPrepareWasm();
        await prepareWasm({ wasmUrl: "/zxing_reader.wasm" });
        await prepareWasm();
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledOnce();
    });
    (0, vitest_1.it)("rejects a different wasm URL after the module is prepared", async () => {
        const prepareWasm = await loadPrepareWasm();
        await prepareWasm({ wasmUrl: "/first.wasm" });
        await (0, vitest_1.expect)(prepareWasm({ wasmUrl: "/second.wasm" })).rejects.toThrow("different wasmUrl");
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledOnce();
    });
    (0, vitest_1.it)("clears a failed preparation so callers can retry", async () => {
        const prepareWasm = await loadPrepareWasm();
        const error = new Error("wasm failed");
        prepareZXingModule
            .mockReturnValueOnce(Promise.reject(error))
            .mockReturnValueOnce(undefined);
        await (0, vitest_1.expect)(prepareWasm()).rejects.toThrow(error);
        await (0, vitest_1.expect)(prepareWasm()).resolves.toBeUndefined();
        (0, vitest_1.expect)(prepareZXingModule).toHaveBeenCalledTimes(2);
    });
});
