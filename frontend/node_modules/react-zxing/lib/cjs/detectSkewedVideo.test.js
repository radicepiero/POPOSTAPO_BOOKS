"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const detectSkewedVideo_1 = require("./detectSkewedVideo");
const createVideo = ({ readyState = HTMLMediaElement.HAVE_ENOUGH_DATA, videoWidth = 0, videoHeight = 0, } = {}) => {
    const video = document.createElement("video");
    Object.defineProperties(video, {
        readyState: { value: readyState },
        videoWidth: { value: videoWidth, configurable: true },
        videoHeight: { value: videoHeight, configurable: true },
    });
    return video;
};
(0, vitest_1.describe)("detectSkewedVideo", () => {
    (0, vitest_1.it)("returns an empty list when the video frame is not ready", async () => {
        const detector = { detect: vitest_1.vi.fn() };
        const video = createVideo();
        await (0, vitest_1.expect)((0, detectSkewedVideo_1.detectSkewedVideo)(video, detector, 15)).resolves.toEqual([]);
        (0, vitest_1.expect)(detector.detect).not.toHaveBeenCalled();
    });
    (0, vitest_1.it)("detects barcodes from a rotated frame", async () => {
        const barcode = { rawValue: "123", format: "ean_13" };
        const detect = vitest_1.vi.fn().mockResolvedValue([barcode]);
        const detector = { detect };
        const video = createVideo({ videoWidth: 40, videoHeight: 20 });
        const context = {
            drawImage: vitest_1.vi.fn(),
            putImageData: vitest_1.vi.fn(),
            translate: vitest_1.vi.fn(),
            rotate: vitest_1.vi.fn(),
            getImageData: vitest_1.vi.fn((width, height) => ({
                width,
                height,
                data: new Uint8ClampedArray(width * height * 4),
            })),
        };
        vitest_1.vi.spyOn(HTMLCanvasElement.prototype, "getContext").mockReturnValue(context);
        await (0, vitest_1.expect)((0, detectSkewedVideo_1.detectSkewedVideo)(video, detector, 10)).resolves.toEqual([
            barcode,
        ]);
        (0, vitest_1.expect)(detect).toHaveBeenCalledOnce();
        (0, vitest_1.expect)(detect.mock.calls[0]?.[0]).toMatchObject({
            width: vitest_1.expect.any(Number),
            height: vitest_1.expect.any(Number),
            data: vitest_1.expect.any(Uint8ClampedArray),
        });
    });
    (0, vitest_1.it)("returns an empty list when canvas context is unavailable", async () => {
        const detector = { detect: vitest_1.vi.fn() };
        const video = createVideo({ videoWidth: 40, videoHeight: 20 });
        vitest_1.vi.spyOn(HTMLCanvasElement.prototype, "getContext").mockReturnValue(null);
        await (0, vitest_1.expect)((0, detectSkewedVideo_1.detectSkewedVideo)(video, detector, 10)).resolves.toEqual([]);
        (0, vitest_1.expect)(detector.detect).not.toHaveBeenCalled();
    });
});
