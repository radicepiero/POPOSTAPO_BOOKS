"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareWasm = void 0;
const ponyfill_1 = require("barcode-detector/ponyfill");
let wasmReady;
const prepareWasm = ({ wasmUrl } = {}) => {
    if (wasmReady) {
        if (wasmUrl !== undefined && wasmReady.wasmUrl !== wasmUrl) {
            return Promise.reject(new Error("prepareWasm() has already been called with a different wasmUrl. " +
                "Call it once with the URL you want before mounting scanners."));
        }
        return wasmReady.promise;
    }
    const promise = (async () => {
        const result = (0, ponyfill_1.prepareZXingModule)({
            ...(wasmUrl
                ? {
                    overrides: {
                        locateFile: (path, prefix) => path.endsWith(".wasm") ? wasmUrl : prefix + path,
                    },
                }
                : {}),
            fireImmediately: true,
        });
        if (result instanceof Promise)
            await result;
    })();
    wasmReady = { wasmUrl, promise };
    promise.catch(() => {
        if (wasmReady?.promise === promise)
            wasmReady = undefined;
    });
    return promise;
};
exports.prepareWasm = prepareWasm;
