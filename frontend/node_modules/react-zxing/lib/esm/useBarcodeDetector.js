import { BarcodeDetector } from "barcode-detector/ponyfill";
import { useMemo } from "react";
export const useBarcodeDetector = ({ formats, } = {}) => useMemo(() => new BarcodeDetector({ ...(formats ? { formats } : {}) }), [formats]);
